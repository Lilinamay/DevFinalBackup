using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class npcDialogues : MonoBehaviour
{
    public dialogueClass[] mydialogues;
    public int[] range;
    // Start is called before the first frame update
    void Start()
    {
        if (plotNumber.Globals.plotNum == 0)
        {
            //mydialogues.
        }




        //mydialogues[0].name[
    }

    // Update is called once per frame
    void Update()
    {
        //mydialogues[plotNumber.Globals.plotNum];
    }
}
